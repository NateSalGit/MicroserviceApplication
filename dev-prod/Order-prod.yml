server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://mysql-db:3306/purchasedb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
